listeners:
  - bind_addr: ":80"
  - bind_addr: ":443"
    https: true

default_upstream:
  host: localhost
  http_port: 8080
  https_port: 8443



upstreams:
<% @host_names.each do |host_name| %>
  - "<%= host_name %>"
<% end %>
